const lasDeLosAngelesParaLlorar = require("./fixtures/tracks-angeles.json");
const elCorazonDeLasDeLosAngelesChidas = require("./fixtures/audio-features-angeles.json");

// console.log(lasDeLosAngelesParaLlorar);
console.log(elCorazonDeLasDeLosAngelesChidas);

// const tracksResponse = { tracks: [] };
// const tracks = tracksResponse.tracks.map(track => track);

// const tracksIds = tracks.map(({ id }) => id );

// const getAudioFeatures;

// const audioFeatures = tracksIds.map(getAudioFeatures);

// const energyAverage = (totalEnergy / tracksIds.length) || 0;